import { style } from "@vanilla-extract/css";

export const wrapperStyles = style({
  maxWidth: "450px",
  width: "90%",
  margin: "0 auto",
  marginTop: 180,
});

export const logoStyles = style({
  display: "block",
  maxWidth: 100,
  margin: "0 auto",
});
